#include "minishell.h"

void	initenv(char **env)
{
	char	**secenv;
	int		i;
	int		j;

	i = 0;
	while (env[i])
	{
		j = 0;
		while (env[i][j])
		{
			
		}
	}
}

void	ft_env(t_lexer *lst, char **env)
{
	initenv(env);
}